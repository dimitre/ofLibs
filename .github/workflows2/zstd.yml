name: zstd

on:
  push:
    paths:
      - .github/workflows/zstd.yml
      - zstd/**

jobs:
  ci:
    uses: ./.github/workflows/_build.yml
    with: { lib: zstd, version: add, meson: true }
    # passes GITHUB_TOKEN automatically
    secrets: inherit
