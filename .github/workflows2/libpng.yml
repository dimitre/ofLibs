name: libpng

on:
  push:
    paths:
      - libpng/**
      - .github/workflows/libpng.yml

jobs:
  ci:
    uses: ./.github/workflows/_build.yml
    with: { lib: libpng, version: v0.12.4 }
    secrets: inherit
