name: mango

on:
  push:
    paths:
      - .github/workflows/mango.yml
      - mango/**

jobs:
  ci:
    uses: ./.github/workflows/_build.yml
    with: { lib: mango, version: v0.12.5, meson: true }
    secrets: inherit
