name: mango

on:
  push:
    tags:
      - "v*"
    paths:
      - mango/**
      - .github/workflows/mango.yml

jobs:
  ci:
    uses: ./.github/workflows/_build.yml
    with: { lib: mango, version: v0.12.4, meson: true }
    secrets: inherit
