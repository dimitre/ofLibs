name: utfcpp

on:
  push:
    paths:
      - utfcpp/**
      - .github/workflows/utfcpp.yml

jobs:
  ci:
    uses: ./.github/workflows/_build.yml
    with: { lib: utfcpp, version: v0.12.3 }
    secrets: inherit
