name: uriparser

on:
  push:
    paths:
      - uriparser/**
      - .github/workflows/uriparser.yml

jobs:
  ci:
    uses: ./.github/workflows/_build.yml
    with: { lib: uriparser, version: v0.12.5 }
    secrets: inherit
