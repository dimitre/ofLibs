name: opencv

on:
  push:
    paths:
      - opencv/**
      - .github/workflows/opencv.yml

jobs:
  ci:
    uses: ./.github/workflows/_build.yml
    with: { lib: opencv, version: v0.12.2 }
    secrets: inherit
