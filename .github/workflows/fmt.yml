name: fmt

on:
  push:
    paths:
      - fmt/**
      - .github/workflows/fmt.yml

jobs:
  ci:
    uses: ./.github/workflows/_build.yml
    with: { lib: fmt, version: v0.12.1 }
    secrets: inherit
    # passes GITHUB_TOKEN automatically
