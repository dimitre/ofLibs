name: zstd

on:
  push:
    paths:
      - zstd/**
      - .github/workflows/zstd.yml

jobs:
  ci:
    uses: ./.github/workflows/_buildwin.yml
    with: { lib: zstd, version: v0.12.3, meson: true }
    # passes GITHUB_TOKEN automatically
    secrets: inherit
