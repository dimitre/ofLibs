name: json

# retrigger 003

on:
  push:
    paths:
      - json/**
      - .github/workflows/json.yml

jobs:
  ci:
    uses: ./.github/workflows/_build.yml
    with: { lib: json, version: v0.12.1 }
    secrets: inherit
