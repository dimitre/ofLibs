name: json

on:
  push:
    tags:
      - "v*"
    paths:
      - json/**
      - .github/workflows/json.yml

jobs:
  ci:
    uses: ./.github/workflows/_build.yml
    with: { lib: json, version: v0.12.4 }
    secrets: inherit
