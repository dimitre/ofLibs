name: tess2

on:
  push:
    paths:
      - .github/workflows/tess2.yml
      - tess2/**

jobs:
  ci:
    uses: ./.github/workflows/_build.yml
    with: { lib: tess2, version: v0.12.5 }
    secrets: inherit
