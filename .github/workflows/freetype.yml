name: freetype

on:
  push:
    paths:
      - freetype/**
      - .github/workflows/freetype.yml

jobs:
  ci:
    uses: ./.github/workflows/_build.yml
    with: { lib: freetype, version: v0.12.5 }
    secrets: inherit
