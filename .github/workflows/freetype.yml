name: freetype

# retrigger 001

on:
  push:
    paths:
      - freetype/**
      - .github/workflows/freetype.yml

jobs:
  ci:
    uses: ./.github/workflows/_build.yml
    with: { lib: freetype, version: v0.12.3 }
    secrets: inherit
