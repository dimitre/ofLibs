name: freetype

on:
  push:
    tags:
      - "v*"
    paths:
      - freetype/**
      - .github/workflows/freetype.yml

jobs:
  ci:
    uses: ./.github/workflows/_build.yml
    with: { lib: freetype, version: v0.12.4 }
    secrets: inherit
