name: zlib-ng

on:
  push:
    paths:
      - zlib-ng/**
      - .github/workflows/zlib-ng.yml

jobs:
  ci:
    uses: ./.github/workflows/_build.yml
    with: { lib: zlib-ng, version: v0.12.1 }
    secrets: inherit
