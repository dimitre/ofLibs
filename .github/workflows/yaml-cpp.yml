name: yaml-cpp
# 001

on:
  push:
    paths:
      - yaml-cpp/**
      - .github/workflows/yaml-cpp.yml

jobs:
  ci:
    uses: ./.github/workflows/_build.yml
    with: { lib: yaml-cpp, version: v0.12.5 }
    secrets: inherit
