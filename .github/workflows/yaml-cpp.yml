name: yaml-cpp

on:
  push:
    paths:
      - yaml-cpp/**
      - .github/workflows/yaml-cpp.yml

jobs:
  ci:
    uses: ./.github/workflows/_build.yml
    with: { lib: yaml-cpp, version: v0.12.1 }
    secrets: inherit
